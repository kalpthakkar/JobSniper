# ============================================================
# JOB SNIPER — Master Configuration (Phase 1-2 Optimizations)
# ============================================================

system:
  # Max parallel polling threads
  # OPTIMIZATION: Increased from 10 to 20 to enable more parallelism
  # With 20 workers and ~5s avg Workday time: 20 / 5 = 4 polls/sec theoretical max
  # Practical: expect 100-120 polls/30s heartbeat
  max_workers: 30

  # NOTE: Polling intervals are now managed by PriorityScheduler with adaptive weighting
  # HIGH:MID:LOW = 3:2:1 slots per cycle. No fixed intervals — frequencies emerge naturally.
  # OPTIMIZATION: base_gap_s reduced from 0.1 → 0.05 in scheduler.py (Phase 1)
  # OPTIMIZATION: max_gap_s reduced from 5.0 → 3.0 in scheduler.py (Phase 1)
  # OPTIMIZATION: per-ATS rate limiting enables independent backoff (Phase 2)

  # IP rotation strategy: none | rotating_proxies | tor | user_agent_rotation
  ip_strategy: user_agent_rotation

  # Path to proxy list file (one proxy per line: http://user:pass@host:port)
  proxy_file: proxies.txt   # optional; leave blank to disable

  # Database file path (local SQLite store)
  # ARCHITECTURE CHOICE: SQLite is optimal for local deployments
  # - Avoids remote DB latency (10-50ms per query → 500ms-2.5s per full cycle with 1000+ companies)
  # - No network costs (crucial with 8000+ tokens polled frequently)
  # - Handles R/W contention via thread-safe locking (RLock in database.py)
  # - Local transaction speed << remote DB RPC overhead
  db_path: data/job_db.db

  # Log level: DEBUG | INFO | WARNING | ERROR
  log_level: INFO

  # Request timeout in seconds
  # CRITICAL: This blocks worker threads. With 40 workers and 30s timeout,
  # if 20% of requests timeout, all 8 workers are blocked for 30s → zero polls happen.
  # Reduced from 30s to 5s to prevent worker thread starvation.
  # Slow endpoints will fail fast and retry from queue rather than blocking entire pool.
  request_timeout: 5

  # Max retries per request before marking company as temporarily unavailable
  # Reduced from 3 to 1: with 5s timeout, 1 retry (2 attempts = 10s) is enough.
  # Prevents any single request from blocking worker for long.
  # Failed requests are queued again by scheduler anyway.
  max_retries: 1

  # Delay between retries (seconds)
  # With request_timeout=5s, retries are fast. Keep this minimal so workers
  # recover quickly from timeouts and move to next request
  retry_delay: 0.1

  # Google Careers polling
  google_enabled: true
  google_cooldown_minutes: 3
  google_request_timeout: 30

  # Tesla Careers polling
  tesla_enabled: true
  tesla_cooldown_minutes: 3
  tesla_request_timeout: 60

  # Apple Careers polling
  apple_enabled: true
  apple_cooldown_minutes: 3
  apple_request_timeout: 20

  # Microsoft Careers polling
  microsoft_enabled: true
  microsoft_cooldown_minutes: 3
  microsoft_request_timeout: 20

  # Disable 24-hour job filter per ATS type
  # Set to true to track ALL jobs regardless of publish date (useful for initial baseline)
  # Set to false (default) to only track jobs published in last 24h (reduces database bloat)
  disable_24h_filter:
    greenhouse: false
    lever: false
    ashby: false
    workable: false
    workday: false

  # Skip detailed job extraction (second HTTP call) per ATS type
  # Set to true to skip fetching full job details, use listing data only (faster but less info)
  # Useful for slow endpoints like Workday where detail fetch adds 5-20s per company
  # Set to false (default) to fetch full details for all new jobs
  skip_detail_extraction:
    greenhouse: false
    lever: false
    ashby: false
    workable: false
    workday: false  # Set to true if throughput is critical and descriptions not needed

  # Frequency inspection: Monitor how often a specific board token is polled
  # Useful for understanding dispatch frequency and slot allocation
  frequency_inspection_enabled: true
  frequency_inspect_board_token: "easthampton"  # e.g., "openai" to track OpenAI's endpoint

  # Performance diagnostics: Enable detailed timing logs for debugging throughput bottlenecks
  # Logs dispatcher stalls, per-poll timing (fetch, extract, db_write), and slow operations
  # Set to false once diagnosis is complete to reduce log noise
  performance_diagnostics_enabled: true

  # Notification channels: now managed via the /notify dashboard UI
  # These values are stored persistently in the database and can be toggled in real-time
  # Legacy support: this list is used as defaults on first startup
  notify_channels:
    - console
    - nats

  # Telegram bot config (only used if 'telegram' in notify_channels)
  telegram:
    bot_token: "8405081173:AAE0YSeRr0ef9476ZLuMAX4eGkxZ1w-Cb04"
    chat_id: "6663005447"

  # Webhook config (only used if 'webhook' in notify_channels)
  webhook:
    url: "https://your-webhook-url.com/notify"
    headers:
      Content-Type: "application/json"

  # NATS config (only used if 'nats' in notify_channels)
  nats:
    servers: ["nats://localhost:4222"]
    subject: "job_sniper.jobs"

# ============================================================
# ATS Endpoint Schemas
# Each ATS type defines how to build the fetch URL + params
# ============================================================
ats_schemas:
  greenhouse:
    base_url: "https://boards-api.greenhouse.io/v1/boards/{board_token}/jobs"
    params:
      content: "true"
    method: GET
    timeout: 30

  ashby:
    base_url: "https://api.ashbyhq.com/posting-api/job-board/{board_token}"
    params:
      includeCompensation: "true"
    method: GET
    timeout: 45  # GraphQL can be slow; increased from default

  workable:
    base_url: "https://apply.workable.com/api/v1/widget/accounts/{board_token}"
    params: {}
    method: GET
    timeout: 40  # REST API with generous timeout

  workday:
    base_url: "{board_token}"
    params: {}
    method: GET
    # page_timeout: timeout for the initial HTML page fetch (short — fast fail)
    # timeout: timeout for the JSON API calls (listing + detail)
    # Both are much shorter than the old 45s value which could stall workers
    # for up to 45×3retries+backoff = 141s per company.
    page_timeout: 10  # initial Workday HTML page fetch
    timeout: 12       # Workday JSON API (listing POST + detail GET)

  lever:
    base_url: "https://api.lever.co/v0/postings/{board_token}"
    params:
      mode: "json"
      limit: "50"
    method: GET
    timeout: 60  # Lever is slow; increased from 30 to handle latency
